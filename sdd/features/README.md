# Features

Cada feature de `Synergi` debe vivir en un subdirectorio propio:

`sdd/features/<feature>/`

Convención mínima:
- `<feature>-spec-v1.md`
- `<feature>-test-plan-v1.md` cuando aplique
- `rules/` o `skills/` locales si la feature lo necesita
